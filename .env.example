# =============================================================================
# SERVICE AUTO GLASS LLC - CONFIGURACIÓN DE VARIABLES DE ENTORNO
# =============================================================================
# Copiar este archivo como .env.local y configurar con valores reales
# cp .env.example .env.local

# =============================================================================
# 🌐 CONFIGURACIÓN DE LA APLICACIÓN
# =============================================================================
# URL base de la aplicación (desarrollo/producción)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# 📊 DIRECTUS CMS - GESTIÓN DE CONTENIDO
# =============================================================================
# URL de tu instancia de Directus CMS
# Desarrollo: http://localhost:8055
# Producción: https://tu-directus-instance.com
NEXT_PUBLIC_DIRECTUS_URL=http://localhost:8055

# Token de acceso para Directus (opcional para contenido público)
# DIRECTUS_TOKEN=tu_token_de_acceso

# =============================================================================
# 📧 AMAZON SES - CONFIGURACIÓN DE EMAIL
# =============================================================================
# Servidor SMTP de Amazon SES (región específica)
# us-east-1: email-smtp.us-east-1.amazonaws.com
# us-west-2: email-smtp.us-west-2.amazonaws.com
# eu-west-1: email-smtp.eu-west-1.amazonaws.com
SMTP_HOST=email-smtp.us-east-1.amazonaws.com

# Puerto SMTP (587 para STARTTLS, 465 para SSL)
SMTP_PORT=587

# Credenciales de Amazon SES SMTP
# Crear en: AWS Console > SES > SMTP Settings > Create SMTP Credentials
SMTP_USER=tu_access_key_id_aqui
SMTP_PASS=tu_secret_access_key_aqui

# Email remitente (debe estar verificado en SES)
SMTP_FROM=no-reply@tudominio.com

# Email destino para notificaciones de la empresa
EMAIL_TO=contacto@tuempresa.com

# =============================================================================
# 🔒 CONFIGURACIÓN DE SEGURIDAD
# =============================================================================
# Hosts permitidos para validación CSRF (separados por comas)
# Ejemplo: localhost:3000,tudominio.com,www.tudominio.com
ALLOWED_ORIGINS=localhost:3000

# Límite de solicitudes por minuto por IP
RATE_LIMIT_REQUESTS=10

# Tiempo de ventana para rate limiting (en minutos)
RATE_LIMIT_WINDOW=1

# =============================================================================
# 🧪 CONFIGURACIÓN DE DESARROLLO
# =============================================================================
# Habilitar logs detallados en desarrollo
NODE_ENV=development

# Habilitar modo debug para emails (imprime en consola sin enviar)
# EMAIL_DEBUG=true

# =============================================================================
# 📝 NOTAS DE CONFIGURACIÓN
# =============================================================================
# 
# 1. AMAZON SES:
#    - Verificar dominio en AWS SES Console
#    - Crear credenciales SMTP específicas
#    - Salir del sandbox mode para envío a emails no verificados
#
# 2. DIRECTUS:
#    - Instalar: npx create-directus-project directus-cms
#    - Configurar colección 'landing_content' con campos apropiados
#    - Habilitar acceso público a la colección si es necesario
#
# 3. SEGURIDAD:
#    - Cambiar ALLOWED_ORIGINS en producción
#    - Usar dominios específicos, no wildcards
#    - Configurar rate limiting según necesidades
#
# 4. PRODUCCIÓN:
#    - Cambiar NODE_ENV=production
#    - Usar URLs HTTPS para NEXT_PUBLIC_APP_URL y DIRECTUS_URL
#    - Verificar todas las variables estén configuradas
#
# =============================================================================
